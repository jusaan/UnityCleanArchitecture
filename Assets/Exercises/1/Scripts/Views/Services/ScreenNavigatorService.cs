using UnityEngine;

namespace UnityExercises.Views.Services
{
    public class ScreenNavigatorService : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}